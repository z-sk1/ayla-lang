fun parse(x int) (int, error) {
    ayla x <= 5 {
        back 42, nil
    }

    back 0, error("oh no")
}

say x int
say err error

x, err = parse(6)

ayla err != nil {
    putln(err)
}

putln(x)
