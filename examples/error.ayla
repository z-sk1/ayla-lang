fun parse(x int) (int, error) {
    ayla x <= 5 {
        give 42, nil
    }

    give 0, error("oh no")
}

say x int
say err error

x, err = parse(6)

ayla err != nil {
    putln(err)
}

putln(x)
