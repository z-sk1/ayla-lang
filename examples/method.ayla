type Person struct {
    Name string
    Age int
}

fun (p Person) getInfo() (string, int) {
    give p.Name, p.Age
}

x := Person{
    Name: "Ziad",
    Age: 13,
}

y := x.getInfo

putln(y())
