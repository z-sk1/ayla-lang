import rl

rl.InitAudio()
rl.InitWindow(1000, 1000, "hi")

m := rl.LoadMusic("megalovania.ogg")

rl.PlayMusic(m)
rl.SetMusicVolume(m, 0.25)

rl.SetTargetFPS(60)

why !rl.ShouldClose() {
    rl.UpdateMusic(m)

    rl.Clear()

    rl.Present()
}

rl.UnloadMusic(m)
rl.CloseAudio()
